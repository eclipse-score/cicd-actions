# *******************************************************************************
# Copyright (c) 2026 Contributors to the Eclipse Foundation
#
# See the NOTICE file(s) distributed with this work for additional
# information regarding copyright ownership.
#
# This program and the accompanying materials are made available under the
# terms of the Apache License Version 2.0 which is available at
# https://www.apache.org/licenses/LICENSE-2.0
#
# SPDX-License-Identifier: Apache-2.0
# *******************************************************************************

name: Warm bazel repository cache
description: Validate and warm Bazel repository cache for all variants.

inputs:
  variants:
    description: |
      Newline-separated list of Bazel targets with an optional leading
      --config=<name> per line (no other --... options are supported).
      Each line is split on whitespace and passed as one fetch invocation.
      Examples: `--config=x86_64-linux //...`, `//...`, `//test //quality`.
    required: true
  dry-run:
    description: |
      When set to true, validates `variants` and skips `bazel fetch`.
      Validation still runs `bazel query`, which may resolve external repositories.
      Accepted values are `true` and `false`.
    required: false
    default: "false"

runs:
  using: composite
  steps:
    - name: Validate variants input
      shell: bash
      env:
        VARIANTS_INPUT: ${{ inputs.variants }}
      run: bash "${{ github.action_path }}/validate_variants.sh" "$VARIANTS_INPUT"

    - name: Warm Bazel repository cache for all variants (with retries)
      shell: bash
      if: ${{ inputs.dry-run != 'true' }}
      env:
        VARIANTS_INPUT: ${{ inputs.variants }}
      run: bash "${{ github.action_path }}/warm_cache.sh" "$VARIANTS_INPUT"
